# Overlay file reference

# Fixes bug with Kyon's speech bubbles during group selection not rendering long text
# actual in-place replacement, not branch-link function
arepl_020C9F00:
    ldr r0, [pc, #0x81C]

# actual in-place replacement, not branch-link function
arepl_020C9FB8:
    ldr r0, [pc, #0x804]

# actual in-place replacement, not branch-link function
arepl_020C9FD4:
    ldr r0, [pc, #0x7E8]